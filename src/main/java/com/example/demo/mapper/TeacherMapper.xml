<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.demo.mapper.TeacherMapper">

    <insert id="insertTeacher" parameterType="teacher" keyProperty="id" useGeneratedKeys="true">
        insert into T_TEACHER (USER_ID, NAME, DEGREE, INTRODUCE, TITLE, PROPORTION)
        values (#{userId}, #{name}, #{degree}, #{introduce}, #{title}, #{proportion})
    </insert>
</mapper>
